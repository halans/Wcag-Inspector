# Server Configuration
# The port number for the application to run on
# If not specified, defaults to 5000
# If the default port is unavailable, the app will automatically find the next available port
PORT=5000

# Analysis Timeout
# Maximum time in milliseconds to wait for remote websites before aborting (default 10000)
ANALYSIS_FETCH_TIMEOUT_MS=10000

# Node Environment
# Set to 'production' for production builds
NODE_ENV=development
